import{clearAllLocalData}from"../services/storage-service.js";import{showToast}from"../components/toast.js";
export function renderPrivacyPage(){const button=document.querySelector("[data-clear-local-data]");if(!button)return;button.addEventListener("click",()=>{if(!confirm("¿Borrar el historial, preferencias, progreso de estudio, Carta del Día y sesión actual de este dispositivo?"))return;const result=clearAllLocalData();showToast(result.ok?"Datos locales borrados":"No fue posible borrar los datos",result.ok?"success":"error");if(result.ok)button.textContent="Datos borrados ✓";});}
